# Setup rotation: "day" for daily, "hour" for hourly and "min" for every minute. Default is "hour"!
LOG_ROTATION_TIME=hour

# Log location
LOG_INFO_DIR=log                # Default is "log"
LOG_INFO_NAME=logger.log        # Save log level greater or equal INFO, default is "logger.log"

LOG_DEBUG_DIR=log-debug         # Default is "log"
LOG_DEBUG_NAME=logger-debug.log # Save log level less than INFO, default is "logger-debug.log"
